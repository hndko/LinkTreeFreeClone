---
export interface Props {
  contact: string;
  phone: string;
  email: string;
}

const { contact, phone, email } = Astro.props;
---

<div>
  <div class="mx-auto max-w-7xl py-8 px-4 text-center sm:px-6 lg:py-12 lg:px-8">
    <h2
      class="text-2xl font-bold tracking-tight text-white/90 sm:text-3xl mb-6"
    >
      <span class="block">{contact}</span>
    </h2>
    <div class="mt-5 flex justify-center gap-4">
      <a
        href={"https://wa.me/" + phone.replace("+", "")}
        class="inline-flex items-center justify-center rounded-xl bg-white/10 backdrop-blur-sm border border-white/10 px-6 py-3 text-base font-medium text-white hover:bg-white/20 transition-all duration-300 hover:scale-105"
      >
        <span class="mr-2">💬</span> WhatsApp
      </a>
      <a
        href={"mailto://" + email}
        class="inline-flex items-center justify-center rounded-xl bg-indigo-600/80 backdrop-blur-sm border border-indigo-500/20 px-6 py-3 text-base font-medium text-white hover:bg-indigo-600 transition-all duration-300 hover:scale-105"
      >
        <span class="mr-2">✉️</span> Email me
      </a>
    </div>
  </div>
</div>
